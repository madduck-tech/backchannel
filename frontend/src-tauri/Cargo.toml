[package]
name = "conversationaly"
version = "1.4.1"
description = "A Tauri App for meeting minutes"
authors = ["Sujith S"]
license = "MIT"
repository = "https://github.com/bykof/conversationaly"
edition.workspace = true
rust-version.workspace = true

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
name = "app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]


# Hardware acceleration, forwarded to transcribe-cpp.
#
# BUILD INSTRUCTIONS:
# ===================
# macOS:     cargo build --release              (Metal, on by default)
#
# Windows:   cargo build --release              (CPU)
#            cargo build --release --features cuda    (NVIDIA GPU)
#            cargo build --release --features vulkan  (AMD/Intel GPU)
#
# Linux:     cargo build --release              (CPU)
#            cargo build --release --features cuda    (NVIDIA GPU)
#            cargo build --release --features rocm    (AMD GPU with ROCm)
#            cargo build --release --features vulkan  (Other GPUs)
#
# QUICK START: Use the helper scripts!
#   ./build-gpu.sh (Unix/macOS)
#   .\build-gpu.ps1 (Windows PowerShell)
#
# Dropped vs the whisper-rs era: `coreml` (transcribe.cpp has no CoreML path;
# Metal covers Apple Silicon) and `openblas`. There is no BLAS feature to declare:
# ggml's CPU path uses tinyBLAS (`GGML_BLAS` is forced OFF upstream), and the host
# decoder links a system BLAS on its own when one is installed, via CMake's
# `TRANSCRIBE_USE_SYSTEM_BLAS`, which defaults to ON. Asking cargo for that feature
# is an error, not a no-op — it broke every Linux CI build until #3.
[features]
default = ["platform-default"]  # Automatically enables best backend per platform

# Platform-appropriate defaults - see target-specific dependencies below
platform-default = []

# Manual GPU acceleration options (for power users to override defaults)
metal = ["transcribe-cpp/metal"]   # macOS: Apple Metal GPU (on by default there)
cuda = ["transcribe-cpp/cuda"]     # Windows/Linux: NVIDIA CUDA GPU
vulkan = ["transcribe-cpp/vulkan"] # Windows/Linux: AMD/Intel Vulkan GPU
rocm = ["transcribe-cpp/rocm"]     # Linux: AMD ROCm

# CPU optimization
openmp = ["transcribe-cpp/openmp"] # OpenMP parallel processing

[build-dependencies]
tauri-build = { version = "2.3.0", features = [] }
reqwest = { version = "0.13", features = ["blocking", "multipart", "json", "stream"] }
which = "8.0"
zip = "8.6"           # ZIP extraction (Windows, macOS)
tar = "0.4"           # TAR extraction (Linux)
xz2 = "0.1"           # XZ decompression (Linux)


[dependencies]
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }
anyhow = "1.0"
base64 = "0.23" # Audio payloads for the Ollama transcription provider
once_cell = "1.17.1"
uuid = { version = "1.0", features = ["v4", "serde"] }

# Cross-platform audio capture
cpal = "0.18.1"

# Wav encoding - now using manual WAV creation instead of hound
# hound = "3.5"

# Dates
chrono = { version = "0.4.31", features = ["serde"] }

# Log
log = "0.4"
tracing = "0.1.40"
which = "8.0"

# Bytes
bytemuck = "1.16.1"

# EBU R128 loudness normalization (professional broadcast standard)
ebur128 = "0.1"

# Noise suppression - RNNoise-based neural network noise reduction
nnnoiseless = "0.5"

# Speech recognition and async utilities
# NOTE: transcribe-cpp is declared in platform-specific sections below
# to ensure correct GPU features are enabled per platform
futures-util = "0.3"
silero_rs = { git = "https://github.com/emotechlab/silero-rs", rev = "26a6460", package = "silero" }

# Async
tokio = { version = "1.32.0", features = ["full", "tracing"] }
tokio-util = "0.7"  # Utilities for tokio including CancellationToken

reqwest = { version = "0.13", features = ["blocking", "multipart", "json", "stream"] }

# Directories
dirs = "6.0"


lazy_static = { version = "1.4.0" }
realfft = "3.4.0"
regex = "1.11.0"
symphonia = { version = "0.6", features = ["aac", "isomp4", "mp3", "flac", "ogg", "vorbis", "pcm", "wav", "opt-simd"] }
rayon = "1.10"
rubato = "5.0"
ringbuf = "0.5"
tempfile = "3.3.0"
whatlang = "0.18"

ffmpeg-sidecar = { git = "https://github.com/nathanbabcock/ffmpeg-sidecar", branch = "main" }

sqlx = { version = "0.9", features = [ "runtime-tokio", "sqlite", "chrono"] }

# Common Tauri configuration
tauri = { version = "2.6.2", features = [ "macos-private-api", "protocol-asset", "tray-icon"] }
tauri-plugin-fs = "2.4.0"
tauri-plugin-dialog = "2.3.0"
tauri-plugin-store = "2.4.0"
tauri-plugin-notification = "2.3.1"
tauri-plugin-updater = "2.3.0"
tauri-plugin-process = "2.3.0"
# Every platform, not just macOS: a Windows release build has no console at all,
# so before this moved out of the macOS block the only persisted diagnostics on
# Windows were zero bytes. See `log_plugin()` in lib.rs.
tauri-plugin-log = { version = "2.6.0", features = ["colored"] }

# Desktop-only single instance guard
[target.'cfg(any(target_os = "macos", windows, target_os = "linux"))'.dependencies]
tauri-plugin-single-instance = "2.4"

# macOS-specific dependencies with Metal GPU acceleration
[target.'cfg(target_os = "macos")'.dependencies]
tauri = { version = "2.6.2", features = ["protocol-asset", "macos-private-api", "tray-icon"] }
once_cell = "1.17.1"
anyhow = "1.0"
time = { version = "0.3", features = ["formatting"] }
reqwest = { version = "0.13", features = ["multipart", "json"] }
cidre = { git = "https://github.com/yury/cidre", rev = "a9587fa", features = ["av", "core_audio"] }
futures-channel = "0.3.31"

# transcribe.cpp — replaces whisper-rs + ort. `metal` is its default feature.
transcribe-cpp = { git = "https://github.com/handy-computer/transcribe.cpp", rev = "553f1099a2b3a5bc4421894be171f09960fc0f3a" }

# Windows-specific dependencies
# Default: CPU-only build (no BLAS)
# Users can enable features manually:
#   --features cuda     (NVIDIA GPUs)
#   --features vulkan   (AMD/Intel GPUs)
#   --features openmp   (CPU threading)
[target.'cfg(target_os = "windows")'.dependencies]
futures-channel = "0.3.31"

# default-features = false: transcribe-cpp defaults to `metal`, which is a
# macOS-only no-op elsewhere but must not be the implied backend here.
# GPU backends come from this crate's cuda/vulkan features (see [features]).
transcribe-cpp = { git = "https://github.com/handy-computer/transcribe.cpp", rev = "553f1099a2b3a5bc4421894be171f09960fc0f3a", default-features = false }

# Linux-specific dependencies
# Default: CPU-only build (no BLAS)
# Users can enable features manually:
#   --features cuda     (NVIDIA GPUs)
#   --features vulkan   (AMD/Intel GPUs)
#   --features rocm     (AMD ROCm)
#   --features openmp   (CPU threading)
[target.'cfg(target_os = "linux")'.dependencies]
futures-channel = "0.3.31"

# cpal's PulseAudio host, enabled on Linux only (#13). Without it cpal falls back to its
# ALSA host, which on a PipeWire machine enumerates no sink monitor at all -- so system
# audio has nothing to select -- and returns one identical description for every input of
# a card, which is #10. The crate behind the feature (`pulseaudio`) is pure Rust and links
# no system library; at runtime it needs a PulseAudio-protocol socket, which pipewire-pulse
# provides, and cpal falls back to ALSA when there is none.
cpal = { version = "0.18.1", features = ["pulseaudio"] }

# See the Windows block for why default-features is off.
transcribe-cpp = { git = "https://github.com/handy-computer/transcribe.cpp", rev = "553f1099a2b3a5bc4421894be171f09960fc0f3a", default-features = false }

[dev-dependencies]
tempfile = "3.3.0"

# NOTE: this file used to carry a [patch.crates-io] for cpal and esaxx-rs.
# Cargo ignores [patch] outside the workspace root, so it never applied and
# every build resolved both from the registry. It was dropped rather than
# hoisted — see the workspace root Cargo.toml for why. Do not re-add it here.
